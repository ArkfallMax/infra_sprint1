[Unit]
Description=gunicorn_kittygram daemon 

After=network.target 

[Service]
User=root

WorkingDirectory=/root/infra_sprint1/backend/

ExecStart=/root/infra_sprint1/backend/venv/bin/gunicorn --bind 0.0.0.0:8080 kittygram_backend.wsgi:application

[Install]
WantedBy=multi-user.target
